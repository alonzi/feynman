\documentclass{article}
\usepackage{feynmp}
\usepackage{pst-pdf}
\pagestyle{empty}

\begin{document}
\unitlength = 1mm
% determine the unit for the size of diagram.

\begin{postscript}
\begin{fmffile}{x}
\begin{fmfgraph*}(80,50)
% Note that the size is given in normal parentheses
% instead of curly brackets.
% Define external vertices from bottom to top

\fmfpen{thick}

% include diagram details here
\ifdefined\todraw
  \input{\todraw}
\else
  \fmfleft{i1,i2}
  \fmfright{o1,o2}
  \fmf{photon}{i1,v1,i2}
  \fmf{fermion}{o1,v1,o2}
  \fmfv{d.sh=circle,d.f=empty,d.si=.5w}{v1}
  \fmffreeze
  \fmfi{plain}{vpath (__o1,__v1) shifted (thick*(1,2))}
  \fmfi{plain}{vpath (__o1,__v1) shifted (thick*(-1,-2))}
  \fmfi{plain}{vpath (__v1,__o2) shifted (thick*(-1,2))}
  \fmfi{plain}{vpath (__v1,__o2) shifted (thick*(1,-2))}
\fi


\end{fmfgraph*}
\end{fmffile}
\end{postscript}

\end{document}
